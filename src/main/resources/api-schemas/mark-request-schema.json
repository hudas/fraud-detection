{
    "title": "Request",
    "description": "Marking as fraudulent request for transaction",
    "type": "object",
    "properties": {
        "transactionId" : {
            "description": "Unique transaction id to prevent duplicated processing",
            "type": "string",
            "minLength": 1
        }
    },
    "required": ["transactionId"],
    "additionalProperties" : false
}
